# Execution Support Matrix

The canonical support matrix and direct eligibility diagnostics are maintained
in {doc}`/streamed_edge_execution`. This reference entry keeps a stable location
for the matrix as the planner expands to MH-1 graph-time selection and LAMMPS
forwarding.
